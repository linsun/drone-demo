apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: frontend-route
  namespace: default
spec:
  parentRefs:
  - name: agentgateway-proxy
    namespace: default
  rules:
  - matches:
    - path:
        type: Prefix
        value: /
    backendRefs:
    - name: frontend
      port: 80
