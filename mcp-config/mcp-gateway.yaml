apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: agentgateway
  namespace: mcp
spec:
  gatewayClassName: agentgateway
  listeners:
  - protocol: HTTP
    port: 3000
    name: http
  - protocol: HTTP
    port: 3001
    name: http-1
